import DynamicHeroText from '../DynamicHeroText';

export default function DynamicHeroTextExample() {
  return (
    <div className="p-8 bg-gradient-to-br from-purple-900 to-pink-900 min-h-[300px] flex items-center justify-center">
      <DynamicHeroText />
    </div>
  );
}
